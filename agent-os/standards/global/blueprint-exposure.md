# Blueprint Exposure

- Only expose properties/functions that designers must tweak or call.
- Prefer read‑only exposure for data not meant to be edited at runtime.
- Use friendly `DisplayName`/tooltips; avoid programmer jargon.
- Keep categories shallow and consistent: `Subsystem|Feature`.
